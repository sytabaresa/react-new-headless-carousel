
export const ExampleElement = ({ n }: { n: number }) => (
    <div className="w-full h-full mx-0 flex items-center justify-center bg-white">
        <h1 className="block text-blue-400 text-2xl font-bold" >{n}</h1>
    </div>
)